import { Code, Note, Link } from 'components'
import { Layout, Playground, Attributes, ParsedCodes } from 'lib/components'

export const meta = {
  title: 'Introduction',
  group: 'Getting Started',
}

## Introduction

A collection of software projects, experiments and code snippets. You can find the source code for all projects on [Github](https://github.com/albertocevallos).

### Projects

- Nothing to see here yet

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
